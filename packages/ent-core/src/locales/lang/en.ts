import component from './en/component';
import common from './en/common';
import sys from './en/sys';

const modules = {
  component,
  common,
  sys
};
export default {
  message: {
    ...modules
  }
};
