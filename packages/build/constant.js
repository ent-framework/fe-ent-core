/**
 * The name of the configuration file entered in the production environment
 */
const GLOB_CONFIG_FILE_NAME = '_app.config.js';

const OUTPUT_DIR = 'dist';

module.exports = { GLOB_CONFIG_FILE_NAME, OUTPUT_DIR };
